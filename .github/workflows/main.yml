name: first workflow

on:
  push: 
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "WELCOME to my GitHu bActions"
      
      - name: checkout repo
        uses: actions/checkout@v5.0.0

      - name: list
        run: ls -la
